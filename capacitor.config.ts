import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.xgen.drummachine',
  appName: 'X-Gen Drum Machine',
  webDir: 'dist'
};

export default config;
